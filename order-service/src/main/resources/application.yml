spring:
  application:
    name: order-service

  datasource:
    url: jdbc:postgresql://localhost:5432/order_db
    username: postgres
    password: bHASKI@2001

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  kafka:
    bootstrap-servers: localhost:9092
#  data:
#    redis:
#      host: localhost
#      port: 6379
server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: health,info